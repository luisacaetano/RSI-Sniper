#!/bin/bash
SCRIPT_DIR="$(cd "$(dirname "$0")/../../../.." && pwd)"
osascript -e "tell application \"Terminal\" to do script \"cd '$SCRIPT_DIR' && python3 install_rsi_sniper.py; echo ''; echo 'Pressione ENTER para fechar...'; read\""
